const initialState = {
    message: "This is the initial state of video-store."
};

const video = (state = initialState, action) => {
    return state;
}


export default video;

// This is a random comment for testing git branch "Items@show@brentongalien981".